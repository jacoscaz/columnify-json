{
  "name": "tabulate-json",
  "version": "0.2.0",
  "description": "A simple, one-dependency CLI tool to render JSON in tabular form",
  "main": "index.js",
  "bin": "./bin.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jacopo Scazzosi <jacopo@scazzosi.com>",
  "license": "MIT",
  "dependencies": {
    "jsonparse": "^1.3.1"
  }
}
